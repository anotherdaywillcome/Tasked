type AssignMembersTriggerSettings = {
	triggerSize: string;
	triggerIconSize: number;
};

export const ASSIGN_MEMBERS_TRIGGER_DEFAULT_SETTINGS = {
	triggerSize: "1.5rem",
	triggerIconSize: 16
} satisfies AssignMembersTriggerSettings;
